---
title: 'Antares Programming'
permalink: /
---
<h1 class="sr-only">{{ title | safe }}</h1>
{% include "banners/latest-video.njk" %}
<section class="section h-feed" aria-labelledby="section--articles">
  <header class="section__header">
    <hgroup role="group">
      <h2 class="section__title p-name" id="section--articles">Articles</h2>
      <p class="section__subtitle">Magbasa.</p>
      <p class="section__description">Mga bagong technology. Mga balita. Mga tutorial. Maraming iba pa.</p>
    </hgroup>
  </header>
  <div class="section__body">
    <ol class="card-list card-list--articles">
      {% for post in collections.posts | reverse %}
        {% if loop.index <= 5 %}
          <li class="card-list__item">
            {% include "ui/article-card.njk" %}
          </li>
        {% endif %}
      {% endfor %}
    </ol>
  </div>
  <div class="section__footer">
    <div class="cluster jc--center">
      <a href="/articles/" class="primary button">Tingnan lahat ng articles</a>
      <a href="{{ site.feeds.articles.url }}" class="button">
        {{ "rss" | icon | safe }}
        <span class="sr-only">RSS feed ng lahat ng articles</span>
      </a>
    </div>
  </div>
</section>