---
title: 'Mga Video - Antares Programming'
# pagination:
#   data: collections.videos
#   size: 10
#   reverse: true
#   generatePageOnEmptyData: true
# permalink: /videos/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber + 1 }}/{% endif %}index.html
permalink: /videos/
---
<section class="section h-feed" aria-labelledby="section--articles">
  <header class="section__header">
    <hgroup role="group">
      <h1 class="section__title p-name" id="section--articles">Videos</h1>
      <p class="section__subtitle">Manood.</p>
      <p class="section__description">Mga coding tutorial at commentaries.</p>
    </hgroup>
  </header>
  <div class="section__body">
    {# {% set headingLevel = 2 %}
    <ol class="card-list card-list--videos">
      {% for post in pagination.items %}
        {% if not post.data.hidden %}
          <li class="card-list__item">
            {% set cardType = 'video' %}
            {% set postType = 'videos' %}
            {% include "ui/article-card.njk" %}
          </li>
        {% endif %}
      {% endfor %}
    </ol> #}

    <p style="text-align: center; font-size: var(--type-size-4);">Wala pang available na videos ngayon.</p>
  </div>
  <footer class="section__footer">
    <nav class="pagination">
      <ul class="pagination__list">
        {% if pagination.href.previous %}
          <li class="pagination__list-item">
            <a href="{{ pagination.href.previous }}" class="button">{{ "arrow-left" | icon | safe }} Previous</a>
          </li>
        {% endif %}
        {% for entry in pagination.pages %}
          <li class="pagination__list-item">
            {% set isCurrent = page.url == pagination.hrefs[loop.index0] %}
            <a class="pagination__link" href="{{ pagination.hrefs[loop.index0] }}" {{ 'aria-current=page' if isCurrent }}>{{ loop.index }}</a>
          </li>
        {% endfor %}
        {% if pagination.href.next %}
          <li class="pagination__list-item">
            <a href="{{ pagination.href.next }}" class="button">{{ "arrow-right" | icon | safe }} Next</a>
          </li>
        {% endif %}
      </ul>
    </nav>
  </footer>
</section>

<footer class="section m-block--xxxl" style="text-align: center">
  <div class="section__header">
    <hgroup role="group">
      <h2 class="section__subtitle h3">Huwag magpahuli sa mga bago naming videos!</h2>
      <p>Maging updated kapag may bago kaming posts. Mag-subscribe sa aming YouTube channel.</p>
    </hgroup>
  </div>
  <div class="stack ai--center gap--s">
    <a href="{{ site.contact.youtube.url }}" class="primary button" target="blank" rel="noopener">
      {{ "youtube" | icon | safe }}
      <span>Antares Programming sa YouTube</span>
    </a>
    <a href="#footer-contact">I-follow din kami sa iba naming social media.</a>
  </div>
</footer>